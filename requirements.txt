# Python test dependencies
# Install with: pip install -r requirements.txt

# Test runner
pytest>=7.0

# Terminal UI for visual scenario monitor
rich>=13.0

# Serial port access (used by ELM327 emulator and integration helpers)
pyserial>=3.5

# NOTE: ELM327-emulator is NOT listed here — it requires setuptools/pkg_resources
# at build time and breaks on Python 3.12+. It is only needed for `make emulator`
# (manual testing without a car) and is installed on demand by that target.
